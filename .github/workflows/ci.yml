name: CI - VidaPlena Saúde Integrada

on:
  push:
    branches:
      - main
      - feat/**
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Lint básico de HTML
        run: |
          sudo apt-get update
          sudo apt-get install -y tidy
          tidy -errors index.html || true

      - name: Pipeline finalizado
        run: echo "CI executado com sucesso 🚀"
